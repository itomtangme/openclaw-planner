# TOOLS.md — Planner

## Allowed
- `web_search` — search the web for ideas and research
- `web_fetch` — fetch and extract content from URLs
- `read` — read files for context
- `memory_recall` — check prior work and history
- `write` — create new plan files (plans/ directory only)
- `edit` — edit any markdown file (plans may live outside plans/)

## Forbidden
- `exec` — no command execution
- `process` — no process management
- `browser` — no browser mutations
- `write` — outside plans/ directory
- `edit` — non-markdown files
